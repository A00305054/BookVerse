<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="BookVerse.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:BookVerse.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="BookVerse">

    

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate views:LoginView}"
        Route="LoginView" />
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate views:HomeView}"
        Route="HomeView" />
    <TabBar>
        <ShellContent Icon="home.png" Route="home" ContentTemplate="{DataTemplate views:HomeView}" />
        <!--<ShellContent Title="Search" Icon="search.png" Route="search" ContentTemplate="{DataTemplate views:SearchView}" />
    <ShellContent Title="Books" Icon="library.png" Route="books" ContentTemplate="{DataTemplate views:BooksView}" />
    <ShellContent Title="Bookmarks" Icon="bookmarks.png" Route="bookmarks" ContentTemplate="{DataTemplate views:BookmarksView}" />
    <ShellContent Title="Notifications" Icon="notifications.png" Route="notifications" ContentTemplate="{DataTemplate views:NotificationsView}" />-->
    </TabBar>

</Shell>
